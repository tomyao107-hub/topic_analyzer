# ── GUI ──────────────────────────────────────────────
PySide6>=6.5.0

# ── 数据处理 ─────────────────────────────────────────
pandas>=2.0.0
numpy>=1.24.0
openpyxl>=3.1.0          # Excel 读写
xlrd>=2.0.0              # .xls 旧格式读取

# ── 中文分词 ─────────────────────────────────────────
jieba>=0.42.1

# ── 主题建模 LDA ──────────────────────────────────────
gensim>=4.3.0

# ── 主题可视化 ────────────────────────────────────────
matplotlib>=3.7.0
pyLDAvis>=3.4.0          # LDA 交互可视化（可选，推荐安装）

# ── STM（通过 rpy2 调用 R）──────────────────────────
# 注意：需要先安装 R (>=4.0) 和 R 包 stm
# 安装 rpy2：pip install rpy2
# 如果不使用 STM，可以不安装 rpy2（取消下行注释即可安装）
rpy2>=3.5.0

# ── 繁简转换（可选）──────────────────────────────────
# 如果不使用繁简转换功能可不安装（取消下行注释即可安装）
opencc-python-reimplemented>=0.1.7
